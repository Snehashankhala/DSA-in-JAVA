public class SumSubstring {
    public static void main(String[] args){
   String s= "6759";
  int sum=0;
  int n= s.length();
  for(int i=0; i<=n; i++){
      for(int j=i+1; j<=n; j++){
          String sub =s.substring(i,j);    #### for this we need to print the i and j so learn the form 
          sum += Integer.parseInt(sub);  ### parseInat do is that the chage the string into an integer type
      }
  }
  System.out.println(sum);
    }
}
  ##ans 
8421
